<%= form_for(@space_rol) do |f| %>
  <% if @space_rol.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@space_rol.errors.count, "error") %> prohibited this space_rol from being saved:</h2>

      <ul>
      <% @space_rol.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
