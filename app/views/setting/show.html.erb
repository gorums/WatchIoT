<div class="row dashboard-page">
	<div class="index-setting">
		<br />
		<div class="panel panel-success">
			<div class="panel-body">
				<h3>Gorums setting</h3>
				<br />

				<%= render "profile", locals: {user: @user, in: @in} %>
				<%= render "account", locals: {user: @user, in: @in} %>
				<%= render "plan", locals: {user: @user, in: @in} %>
				<%= render "team", locals: {user: @user, in: @in} %>
				<%= render "security", locals: {user: @user, in: @in} %>
				<%= render "api_key", locals: {user: @user, in: @in} %>
			</div>
		</div>
  </div>
</div>
